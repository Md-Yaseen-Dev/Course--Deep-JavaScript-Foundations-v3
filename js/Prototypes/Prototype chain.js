
        // prototype
// there is one critical entity i.e function  and go by a pretty familar name,it goes by capital O, object.
// object have object.name, object.value

// so we create another object and there is no name for it.
// so we name  it,is refferring to what points at it.
// Over at this thing and that property is called prototype
// example object. prototype points pointing from object function to object.

       //  Constructor
// property pointing back the other  direction  is called constructor
//constructor deals with class system.