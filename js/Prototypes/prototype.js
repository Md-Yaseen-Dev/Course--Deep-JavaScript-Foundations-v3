//  A constructor call makes an object based on its own prototype

//  A constructor call makes an object linked to its own prototype.

// Protypes are the mechanism by which javascript objects inheritance from one another. in this article we explain what a prototype is how