// Object.is(..)should take two parameters
// It should return true if the passed in parameters are exactly the same value (not just '===')or false otherwise.
// for NaN testing you can use Number.isNaN(..), but first see if you can fine a way to test without usage of any utility\
// for -0 testing  no built-in utility exists ,but here's a hint -infinty
// If the parameters are any other values, just test them for strict equality
// you cannot use the buil;t-in 'object.is(..)'-- thats cheating
